const parser = require('../../util/parser')

const chapters = parser.chapters
const characters = parser.characters

module.exports = {
  chapters: chapters,
  characters: characters
}

//console.log(game.chapters[0].scenes[0])
